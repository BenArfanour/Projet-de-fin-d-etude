{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, { 'action': path('fos_user_change_password'), 'attr': { 'class': 'fos_user_change_password' } }) }}
    <h1> Changer Votre Mot de passe acutel </h1>
    {{ form_widget(form) }}
  <br>  <div>
      <center>  <input type="submit" value="{{ 'change_password.submit'|trans }}" class="btn btn-danger" /> </center>
    </div>
{{ form_end(form) }}
